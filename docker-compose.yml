services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: sasci
      POSTGRES_PASSWORD: sasci
      POSTGRES_DB: sasci
    ports:
      - "5432:5432"
    volumes:
      - sasci-db-data:/var/lib/postgresql/data

volumes:
  sasci-db-data:
